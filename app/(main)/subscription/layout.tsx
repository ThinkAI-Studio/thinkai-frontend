import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Gói thành viên Lumina Pro',
  description: 'Nâng cấp gói thành viên Lumina Pro để mở khóa toàn bộ tính năng và gia sư AI không giới hạn.',
};

export default function SubscriptionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
